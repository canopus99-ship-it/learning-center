# 피아노교실 등급 승급 기능 - 적용 방법

## 1단계: Supabase에서 SQL 실행 (필수, 먼저 하기)

1. supabase.com 접속 → 로그인
2. 이 프로젝트(늘품학습센터/learning-center) 클릭
3. 왼쪽 메뉴 아래쪽 톱니바퀴 아이콘 → **SQL Editor** 클릭
4. **New query** 클릭 후, 아래 SQL을 그대로 붙여넣고 **Run** 클릭

```sql
-- 1) 결제 시점의 등급을 저장할 컬럼 추가
--    (승급 이후에도 이미 낸 결제건은 그때 등급 그대로 유지하기 위함)
alter table payments
  add column if not exists course_level_id bigint references course_levels(id);

-- 2) 승급 이력을 남기는 테이블
create table if not exists course_level_changes (
  id bigint generated by default as identity primary key,
  enrollment_id bigint not null references enrollments(id) on delete cascade,
  from_level_id bigint references course_levels(id),
  to_level_id bigint not null references course_levels(id),
  changed_at timestamptz not null default now()
);
```

실행 후 에러 없이 "Success"가 뜨면 완료입니다. (이미 실행한 뒤 다시 실행해도 안전합니다 - `if not exists` 처리되어 있음)

## 2단계: 코드 배포

기존과 동일하게 이 zip 내용을 깃허브 폴더에 덮어쓰고 commit/push 하시면 Vercel이 자동 재배포합니다.

## 3단계: 확인 및 사용법

- **수납관리** 화면 (강좌별 보기 탭)과 **회원상세** 페이지 양쪽에, 등급 강좌(피아노교실 등)를 듣는 회원 이름 옆에 현재 등급 배지가 표시되고, "🎓 등급변경" 버튼이 생깁니다.
- 승급시킬 회원의 "🎓 등급변경" 버튼 클릭 → 새 등급 선택 → 저장. 기존 수강신청을 종료하거나 다시 등록할 필요 없이 그 자리에서 등급만 바뀝니다.
- 등급을 바꿔도 **이미 인쇄했던 영수증·일별결제현황은 변경 전 등급(예: 초급) 그대로** 남습니다. 승급 이후 새로 등록되는 결제부터 새 등급(예: 중급)이 적용됩니다.
- 회원상세 페이지의 해당 강좌 아래 "승급 이력 (N)"을 펼치면 언제 어떤 등급에서 어떤 등급으로 바뀌었는지 확인할 수 있습니다.

## 참고

- 등급이 없는(무료 강좌, 일반 강좌 등) 수강신청에는 이 버튼이 나타나지 않습니다. 강좌관리에서 "등급별 수강료 사용"으로 설정된 강좌(피아노교실 등)에서만 나타납니다.
- 1단계 SQL을 실행하기 전에 코드만 먼저 배포해도 화면이 깨지지는 않지만, 등급을 변경해도 이력이 저장되지 않고 결제 시 등급 스냅샷도 되지 않습니다. 반드시 SQL을 먼저 실행해주세요.
